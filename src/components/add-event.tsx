import React from "react";

interface Props{
    currentDate: Date;
}

const AddEvent: React.FC<Props> = (currentDate) => {

    return(
        <div>
            
        </div>
    );

};

export default AddEvent;